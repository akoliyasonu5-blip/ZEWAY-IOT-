import test from 'node:test';import assert from 'node:assert/strict';import {encode,parse,location,ack} from './jt808.js';test('framing, escapes and acknowledgement',()=>{const body=Buffer.from([0x7e,0x7d]);const msg=parse(encode(0x0002,'123456789012',5,body));assert.equal(msg.terminal,'123456789012');assert.deepEqual(msg.body,body);assert.equal(parse(ack(msg)).id,0x8001)});test('GPS coordinates, speed and time',()=>{const b=Buffer.alloc(28);b.writeUInt32BE(3,4);b.writeUInt32BE(28580000,8);b.writeUInt32BE(77320000,12);b.writeUInt16BE(325,18);Buffer.from('260924130000','hex').copy(b,22);const pos=location(parse(encode(0x0200,'123456789012',1,b)));assert.equal(pos.lat,28.58);assert.equal(pos.lng,77.32);assert.equal(pos.speed,32.5);assert.equal(pos.gpsValid,true)});
test('2019 version header and response',()=>{const msg=parse(encode(0x0002,'12345678901234567890',6,Buffer.alloc(0),true));assert.equal(msg.versioned,true);assert.equal(msg.terminal,'12345678901234567890');assert.equal(parse(ack(msg)).versioned,true)});
